services:
  db:
    image: postgres:16
    container_name: db-togo
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=togo_test
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
